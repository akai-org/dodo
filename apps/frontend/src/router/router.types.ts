export enum Route {
    BASE = "/",
    HOME = "/home",
    CALENDAR = "/calendar",
    TODOLIST = '/todolist',
    CATEGORIES = '/categories',
    NOTES = '/notes',
    SETTINGS = '/settings',
    LOGIN = '/login',
    NOT_FOUND = '/*',
}

export enum SETTINGS_PATH {
    THEMES= "/themes",
    CALENDAR = "/calendar",
    TODOLIST = "/todolist",
    NOTES = "/notes",
    PROFILE = "/profile",
}
